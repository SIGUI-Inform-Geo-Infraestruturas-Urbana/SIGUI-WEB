import { ShowUserAdmLoggedDirective } from './show-user-adm-logged.directive';

describe('ShowUserAdmLoggedDirective', () => {
  it('should create an instance', () => {
    //const directive = new ShowUserAdmLoggedDirective();
    //expect(directive).toBeTruthy();
  });
});
